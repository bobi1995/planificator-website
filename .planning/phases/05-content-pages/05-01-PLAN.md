# 05-01-PLAN.md -- Features Page

## Frontmatter

| Key | Value |
|-----|-------|
| wave | 1 |
| depends_on | (none) |
| files_modified | `src/components/ui/card.tsx`, `src/components/ui/badge.tsx`, `src/components/ui/separator.tsx`, `src/components/ui/tabs.tsx`, `src/components/ui/accordion.tsx`, `src/components/ui/table.tsx`, `messages/en.json`, `messages/bg.json`, `src/components/sections/features/FeatureHero.tsx`, `src/components/sections/features/FeatureDomainSection.tsx`, `src/app/[locale]/features/page.tsx` |
| autonomous | true |

## Objective

Install the 6 new shadcn/ui components needed across all Phase 5 pages (Card, Badge, Separator, Tabs, Accordion, Table), then build the Features page -- the largest and most content-rich content page. The Features page replaces the current "Coming Soon" stub with a scrollable page organized into 6 feature domains (Scheduling, Resource Management, Optimization, Shifts/Calendar, BOM/Inventory, Shop Floor), each with a domain-level heading, description, and a grid of 3-4 feature cards. Future features (BOM, Inventory, Shop Floor) are presented as existing per STATE.md decision. The page ends with the reusable CTABanner.

Purpose: The Features page is the primary content destination from the landing page's "See Features" CTA and header navigation. It must communicate every product capability to manufacturing decision-makers in both EN and BG.

Output: Fully built Features page with ~90 translation keys per locale, 2 new section components, and 6 shadcn/ui components available for subsequent plans.

## Tasks

<task id="1">
<title>Install shadcn/ui components and add FeaturesPage translations</title>
<instructions>

**Step 1: Install 6 shadcn/ui components.**

Run these commands from the project root:

```bash
npx shadcn@latest add card
npx shadcn@latest add badge
npx shadcn@latest add separator
npx shadcn@latest add tabs
npx shadcn@latest add accordion
npx shadcn@latest add table
```

Each command creates a file in `src/components/ui/`. Verify all 6 new files exist after installation.

**Step 2: Add `FeaturesPage` namespace to `messages/en.json`.**

Add the following namespace at the end of the JSON (before the closing brace). This contains all content for the Features page -- 6 domains with 3-4 features each, plus page-level hero and metadata.

```json
"FeaturesPage": {
  "metaTitle": "Features",
  "metaDescription": "Explore Planifactor's production scheduling features: AI optimization, Gantt visualization, resource management, shift planning, BOM, and shop floor tracking.",
  "hero": {
    "title": "Powerful Tools for Modern Manufacturing",
    "subtitle": "From AI-powered scheduling to real-time shop floor tracking, Planifactor gives your team complete control over production planning."
  },
  "domains": {
    "scheduling": {
      "title": "Production Scheduling",
      "description": "Visualize, plan, and optimize your entire production timeline with an intelligent Gantt chart interface.",
      "icon": "GanttChart",
      "features": {
        "0": {
          "title": "Interactive Gantt Chart",
          "description": "See every order, resource, and operation on a dynamic timeline. Drag and drop to reschedule, zoom in on bottlenecks, and get the big picture at a glance."
        },
        "1": {
          "title": "Drag-and-Drop Scheduling",
          "description": "Move operations between resources and time slots with intuitive drag-and-drop. Conflicts are detected automatically before you commit changes."
        },
        "2": {
          "title": "Real-Time Conflict Detection",
          "description": "Instantly see scheduling conflicts, resource overloads, and constraint violations. Planifactor highlights problems before they reach the shop floor."
        },
        "3": {
          "title": "Multi-Resource View",
          "description": "View all machines, workers, and workstations on a single timeline. Filter by resource group, shift, or department to focus on what matters."
        }
      }
    },
    "resources": {
      "title": "Resource Management",
      "description": "Track every machine, worker, and station. Know what is available, what is running, and what needs attention.",
      "icon": "Users",
      "features": {
        "0": {
          "title": "Machine & Equipment Tracking",
          "description": "Register all production resources with detailed specifications -- capacity, capabilities, setup requirements, and maintenance windows."
        },
        "1": {
          "title": "Worker & Skill Management",
          "description": "Assign workers based on skills, certifications, and availability. Ensure the right person is at the right machine at the right time."
        },
        "2": {
          "title": "Capacity Planning",
          "description": "See available capacity across all resources at a glance. Identify bottlenecks before they cause delays and plan for peak demand."
        },
        "3": {
          "title": "Resource Groups",
          "description": "Organize resources into logical groups -- production lines, departments, or skill pools. Schedule at the group level or drill down to individual resources."
        }
      }
    },
    "optimization": {
      "title": "AI Optimization",
      "description": "Let artificial intelligence find the best production schedule. Configure your goals and let the optimizer do the heavy lifting.",
      "icon": "Brain",
      "features": {
        "0": {
          "title": "AI Scheduling Engine",
          "description": "Our optimization engine evaluates thousands of possible schedules to find the one that best meets your objectives -- automatically."
        },
        "1": {
          "title": "Configurable Objectives",
          "description": "Minimize late orders, reduce changeover time, balance workload, or optimize for throughput. Set priorities and let the AI decide the best trade-offs."
        },
        "2": {
          "title": "Constraint Handling",
          "description": "Define hard constraints (resource availability, order deadlines) and soft constraints (preferred sequences, load balancing). The optimizer respects them all."
        },
        "3": {
          "title": "What-If Scenarios",
          "description": "Run multiple optimization scenarios to compare outcomes. See how adding a shift, changing priorities, or accepting a rush order affects your entire schedule."
        }
      }
    },
    "shifts": {
      "title": "Shifts & Calendar",
      "description": "Define work patterns, manage breaks, and handle changeovers. Your schedule reflects how your factory actually operates.",
      "icon": "Calendar",
      "features": {
        "0": {
          "title": "Shift Templates",
          "description": "Create reusable shift patterns -- day shift, night shift, rotating schedules. Apply them to resources or entire departments with one click."
        },
        "1": {
          "title": "Break & Downtime Management",
          "description": "Schedule breaks, planned maintenance, and cleaning periods. The optimizer works around downtime automatically."
        },
        "2": {
          "title": "Changeover Time Matrices",
          "description": "Define setup times between product types or configurations. The optimizer sequences jobs to minimize total changeover time."
        },
        "3": {
          "title": "Holiday & Exception Calendars",
          "description": "Mark public holidays, factory shutdowns, and special events. The schedule adjusts automatically -- no manual corrections needed."
        }
      }
    },
    "bom": {
      "title": "BOM & Inventory",
      "description": "Manage bills of materials, track stock levels, and ensure materials are available when production needs them.",
      "icon": "Layers",
      "features": {
        "0": {
          "title": "Bill of Materials",
          "description": "Define multi-level BOMs with components, sub-assemblies, and raw materials. Link BOMs directly to production orders for full traceability."
        },
        "1": {
          "title": "Inventory Tracking",
          "description": "Monitor stock levels in real time. Get alerts when inventory drops below safety stock and see which orders are at risk of material shortages."
        },
        "2": {
          "title": "Material Availability Check",
          "description": "Before scheduling an order, verify that all required materials are in stock or on order. Prevent scheduling orders that cannot be produced."
        }
      }
    },
    "shopFloor": {
      "title": "Shop Floor Tracking",
      "description": "See what is happening on the production floor right now. Track progress, collect feedback, and close the loop between planning and execution.",
      "icon": "Activity",
      "features": {
        "0": {
          "title": "Real-Time Production Status",
          "description": "See which operations are in progress, completed, or delayed. Live updates from the shop floor feed directly into your scheduling view."
        },
        "1": {
          "title": "Order Progress Tracking",
          "description": "Track each order from release to completion. Know exactly where every job stands and when it will be finished."
        },
        "2": {
          "title": "Operator Feedback",
          "description": "Let operators report progress, flag issues, and log actual times from the production floor. Close the gap between the plan and reality."
        }
      }
    }
  }
}
```

**Step 3: Add `FeaturesPage` namespace to `messages/bg.json`.**

Add the Bulgarian translation at the same position:

```json
"FeaturesPage": {
  "metaTitle": "Функции",
  "metaDescription": "Разгледайте функциите на Planifactor за производствено планиране: AI оптимизация, Гант визуализация, управление на ресурси, планиране на смени, BOM и проследяване на цеха.",
  "hero": {
    "title": "Мощни инструменти за модерно производство",
    "subtitle": "От AI задвижвано планиране до проследяване на цеха в реално време -- Planifactor дава на вашия екип пълен контрол над производственото планиране."
  },
  "domains": {
    "scheduling": {
      "title": "Производствено Планиране",
      "description": "Визуализирайте, планирайте и оптимизирайте цялата си производствена времева линия с интелигентен Гант интерфейс.",
      "icon": "GanttChart",
      "features": {
        "0": {
          "title": "Интерактивна Гант Диаграма",
          "description": "Вижте всяка поръчка, ресурс и операция на динамична времева линия. Влачете и пускайте за препланиране, увеличавайте за тесни места и получете общата картина с един поглед."
        },
        "1": {
          "title": "Планиране с Влачене и Пускане",
          "description": "Местете операции между ресурси и времеви слотове с интуитивно влачене и пускане. Конфликтите се откриват автоматично преди да потвърдите промените."
        },
        "2": {
          "title": "Откриване на Конфликти в Реално Време",
          "description": "Мигновено виждайте конфликти в графика, претоварване на ресурси и нарушения на ограничения. Planifactor подчертава проблемите преди да стигнат до цеха."
        },
        "3": {
          "title": "Изглед на Множество Ресурси",
          "description": "Вижте всички машини, работници и работни станции на единна времева линия. Филтрирайте по група ресурси, смяна или отдел за фокус върху важното."
        }
      }
    },
    "resources": {
      "title": "Управление на Ресурси",
      "description": "Проследявайте всяка машина, работник и станция. Знайте какво е налично, какво работи и какво се нуждае от внимание.",
      "icon": "Users",
      "features": {
        "0": {
          "title": "Проследяване на Машини и Оборудване",
          "description": "Регистрирайте всички производствени ресурси с детайлни спецификации -- капацитет, възможности, изисквания за настройка и прозорци за поддръжка."
        },
        "1": {
          "title": "Управление на Работници и Умения",
          "description": "Разпределяйте работници по умения, сертификати и наличност. Осигурете правилния човек на правилната машина в правилното време."
        },
        "2": {
          "title": "Планиране на Капацитет",
          "description": "Вижте наличния капацитет на всички ресурси с един поглед. Идентифицирайте тесните места преди да причинят забавяния и планирайте за пикови натоварвания."
        },
        "3": {
          "title": "Групи Ресурси",
          "description": "Организирайте ресурси в логични групи -- производствени линии, отдели или пулове от умения. Планирайте на ниво група или навлезте в детайли за отделни ресурси."
        }
      }
    },
    "optimization": {
      "title": "AI Оптимизация",
      "description": "Оставете изкуствения интелект да намери най-добрия производствен график. Конфигурирайте целите си и оставете оптимизатора да свърши тежката работа.",
      "icon": "Brain",
      "features": {
        "0": {
          "title": "AI Двигател за Планиране",
          "description": "Нашият оптимизационен двигател оценява хиляди възможни графици, за да намери този, който най-добре отговаря на вашите цели -- автоматично."
        },
        "1": {
          "title": "Конфигурируеми Цели",
          "description": "Минимизирайте закъснелите поръчки, намалете времето за смяна, балансирайте натоварването или оптимизирайте за производителност. Задайте приоритети и оставете AI да реши най-добрите компромиси."
        },
        "2": {
          "title": "Обработка на Ограничения",
          "description": "Определете твърди ограничения (наличност на ресурси, срокове за поръчки) и меки ограничения (предпочитани последователности, балансиране на натоварването). Оптимизаторът спазва всички."
        },
        "3": {
          "title": "Сценарии \"Какво Ако\"",
          "description": "Стартирайте множество сценарии за оптимизация, за да сравните резултатите. Вижте как добавянето на смяна, промяната на приоритети или приемането на спешна поръчка влияе на целия ви график."
        }
      }
    },
    "shifts": {
      "title": "Смени и Календар",
      "description": "Определете работни модели, управлявайте почивки и обработвайте смяна на инструменти. Графикът ви отразява как фабриката ви реално работи.",
      "icon": "Calendar",
      "features": {
        "0": {
          "title": "Шаблони за Смени",
          "description": "Създайте многократно използваеми модели на смени -- дневна, нощна, ротационни графици. Приложете ги към ресурси или цели отдели с едно щракване."
        },
        "1": {
          "title": "Управление на Почивки и Престои",
          "description": "Планирайте почивки, планирана поддръжка и периоди за почистване. Оптимизаторът работи около престоите автоматично."
        },
        "2": {
          "title": "Матрици за Време на Смяна",
          "description": "Определете времена за настройка между типове продукти или конфигурации. Оптимизаторът подрежда задачите, за да минимизира общото време за смяна."
        },
        "3": {
          "title": "Празнични и Изключителни Календари",
          "description": "Маркирайте държавни празници, спиране на фабриката и специални събития. Графикът се коригира автоматично -- без нужда от ръчни корекции."
        }
      }
    },
    "bom": {
      "title": "BOM и Инвентар",
      "description": "Управлявайте структурни ведомости за материали, проследявайте нива на запасите и осигурявайте наличност на материали, когато производството ги нуждае.",
      "icon": "Layers",
      "features": {
        "0": {
          "title": "Структурна Ведомост за Материали",
          "description": "Определете многостепенни BOM с компоненти, подвъзли и суровини. Свържете BOM директно с производствени поръчки за пълна проследимост."
        },
        "1": {
          "title": "Проследяване на Инвентар",
          "description": "Наблюдавайте нива на запасите в реално време. Получавайте известия, когато инвентарът падне под минималния запас и вижте кои поръчки са в риск от липса на материали."
        },
        "2": {
          "title": "Проверка на Наличност на Материали",
          "description": "Преди планиране на поръчка, проверете дали всички необходими материали са налични или поръчани. Предотвратете планирането на поръчки, които не могат да бъдат произведени."
        }
      }
    },
    "shopFloor": {
      "title": "Проследяване на Цеха",
      "description": "Вижте какво се случва на производствения етаж точно сега. Проследявайте напредъка, събирайте обратна връзка и затваряйте кръга между планиране и изпълнение.",
      "icon": "Activity",
      "features": {
        "0": {
          "title": "Статус на Производството в Реално Време",
          "description": "Вижте кои операции са в ход, завършени или забавени. Актуализации на живо от цеха се отразяват директно в изгледа за планиране."
        },
        "1": {
          "title": "Проследяване на Напредъка на Поръчки",
          "description": "Проследявайте всяка поръчка от пускане до завършване. Знайте точно къде стои всяка задача и кога ще бъде приключена."
        },
        "2": {
          "title": "Обратна Връзка от Оператори",
          "description": "Позволете на операторите да докладват напредък, да сигнализират за проблеми и да записват реални времена от производствения етаж. Затворете разликата между плана и реалността."
        }
      }
    }
  }
}
```

**IMPORTANT:** After adding both namespaces, verify that both JSON files are valid by running:
```bash
node -e "JSON.parse(require('fs').readFileSync('messages/en.json','utf8'))"
node -e "JSON.parse(require('fs').readFileSync('messages/bg.json','utf8'))"
```

Both must succeed without errors.
</instructions>
<verify>
1. All 6 shadcn/ui component files exist in `src/components/ui/`: `card.tsx`, `badge.tsx`, `separator.tsx`, `tabs.tsx`, `accordion.tsx`, `table.tsx`
2. `node -e "JSON.parse(require('fs').readFileSync('messages/en.json','utf8'))"` succeeds
3. `node -e "JSON.parse(require('fs').readFileSync('messages/bg.json','utf8'))"` succeeds
4. Both locale files contain a `FeaturesPage` namespace with `hero`, `domains` (6 domains), and `metaTitle`/`metaDescription`
</verify>
</task>

<task id="2" depends_on="1">
<title>Create FeatureHero and FeatureDomainSection components, replace Features page stub</title>
<instructions>

**File 1: `src/components/sections/features/FeatureHero.tsx`**

Async server component. Renders the Features page hero: a centered heading + subtitle on a light muted background.

```typescript
import {getTranslations} from 'next-intl/server';

export async function FeatureHero() {
  const t = await getTranslations('FeaturesPage');

  return (
    <section className="py-20 px-4 bg-muted/30">
      <div className="max-w-4xl mx-auto text-center">
        <h1 className="text-display md:text-hero">
          {t('hero.title')}
        </h1>
        <p className="mt-6 text-body-lg text-muted-foreground max-w-3xl mx-auto">
          {t('hero.subtitle')}
        </p>
      </div>
    </section>
  );
}
```

Note: Use `text-display md:text-hero` for the page H1 (bigger than section H2s which use `text-heading md:text-display`). This establishes hierarchy between page title and section titles.

---

**File 2: `src/components/sections/features/FeatureDomainSection.tsx`**

Async server component. This is the key reusable component -- it accepts a `domain` prop and renders the domain title, description, and a grid of feature cards. Each domain renders differently based on even/odd index for visual variety (alternating background).

```typescript
import {getTranslations} from 'next-intl/server';
import {
  GanttChart, Users, Brain, Calendar, Layers, Activity,
  MousePointerClick, AlertTriangle, Monitor, Wrench, GraduationCap,
  BarChart3, FolderTree, Zap, SlidersHorizontal, GitBranch,
  Clock, Coffee, ArrowLeftRight, CalendarDays,
  FileStack, Package, ClipboardCheck,
  Radio, ListChecks, MessageSquare
} from 'lucide-react';

// Map domain keys to their Lucide header icons
const domainIcons: Record<string, React.ComponentType<{className?: string}>> = {
  scheduling: GanttChart,
  resources: Users,
  optimization: Brain,
  shifts: Calendar,
  bom: Layers,
  shopFloor: Activity,
};

// Map domain + feature index to specific Lucide icons for feature cards
const featureIcons: Record<string, React.ComponentType<{className?: string}>[]> = {
  scheduling: [Monitor, MousePointerClick, AlertTriangle, GanttChart],
  resources: [Wrench, GraduationCap, BarChart3, FolderTree],
  optimization: [Zap, SlidersHorizontal, GitBranch, GanttChart],
  shifts: [Clock, Coffee, ArrowLeftRight, CalendarDays],
  bom: [FileStack, Package, ClipboardCheck],
  shopFloor: [Radio, ListChecks, MessageSquare],
};

interface Props {
  domain: string;
  index: number;
}

export async function FeatureDomainSection({domain, index}: Props) {
  const t = await getTranslations(`FeaturesPage.domains.${domain}`);

  const DomainIcon = domainIcons[domain] || GanttChart;
  const icons = featureIcons[domain] || [];

  // Count features (domains have 3 or 4 features)
  const featureCount = domain === 'bom' || domain === 'shopFloor' ? 3 : 4;
  const featureIndices = Array.from({length: featureCount}, (_, i) => i);

  const isAlternate = index % 2 === 1;

  return (
    <section className={isAlternate ? 'py-20 px-4 bg-muted/30' : 'py-20 px-4'}>
      <div className="max-w-7xl mx-auto">
        {/* Domain header */}
        <div className="mb-12">
          <div className="flex items-center gap-3 mb-4">
            <DomainIcon className="h-10 w-10 text-brand-600" />
            <h2 className="text-heading md:text-display">{t('title')}</h2>
          </div>
          <p className="text-body-lg text-muted-foreground max-w-3xl">
            {t('description')}
          </p>
        </div>

        {/* Feature cards grid */}
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {featureIndices.map((i) => {
            const FeatureIcon = icons[i] || GanttChart;
            return (
              <div
                key={i}
                className="rounded-lg border bg-card p-6 transition-colors hover:bg-accent"
              >
                <FeatureIcon className="h-8 w-8 text-brand-600 mb-3" />
                <h3 className="text-subheading mb-2">
                  {t(`features.${i}.title`)}
                </h3>
                <p className="text-muted-foreground">
                  {t(`features.${i}.description`)}
                </p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
}
```

Important details:
- `index` prop is used for alternating backgrounds (`bg-muted/30` on odd-indexed domains)
- Feature card grid uses `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` (1 on mobile, 2 on tablet, 3 on desktop)
- Card pattern matches established pattern from FeatureHighlights: `rounded-lg border bg-card p-6 transition-colors hover:bg-accent`
- Icons are resolved from static maps (NOT passed as props from server to client -- this component IS a server component, so no serialization boundary)
- `bom` and `shopFloor` have 3 features; the other 4 domains have 4 features each

---

**File 3: Replace `src/app/[locale]/features/page.tsx`**

Replace the entire stub page with the full Features page:

```typescript
import {setRequestLocale, getTranslations} from 'next-intl/server';
import {FeatureHero} from '@/components/sections/features/FeatureHero';
import {FeatureDomainSection} from '@/components/sections/features/FeatureDomainSection';
import {CTABanner} from '@/components/sections/CTABanner';

type Props = {
  params: Promise<{locale: string}>;
};

export async function generateMetadata({params}: Props) {
  const {locale} = await params;
  const t = await getTranslations({locale, namespace: 'FeaturesPage'});
  return {
    title: `${t('metaTitle')} | Planifactor`,
    description: t('metaDescription'),
  };
}

const domains = [
  'scheduling',
  'resources',
  'optimization',
  'shifts',
  'bom',
  'shopFloor',
] as const;

export default async function FeaturesPage({params}: Props) {
  const {locale} = await params;
  setRequestLocale(locale);

  return (
    <>
      <FeatureHero />
      {domains.map((domain, index) => (
        <FeatureDomainSection key={domain} domain={domain} index={index} />
      ))}
      <CTABanner />
    </>
  );
}
```

The page is a composition root that maps over the 6 domains. Each `FeatureDomainSection` is an async server component that fetches its own translations. The page ends with CTABanner (reused from landing page).

Create the `src/components/sections/features/` directory before creating the component files.
</instructions>
<verify>
1. Run `npm run build` -- must succeed with zero errors
2. Run `npm run dev` and visit `http://localhost:3000/en/features`:
   - Hero section with "Powerful Tools for Modern Manufacturing" heading
   - 6 domain sections (Scheduling, Resource Management, AI Optimization, Shifts & Calendar, BOM & Inventory, Shop Floor Tracking)
   - Each domain section has a header with icon + title + description
   - Each domain has 3-4 feature cards in a responsive grid
   - Alternating background colors on odd-indexed domains
   - CTABanner at the bottom
3. Visit `http://localhost:3000/bg/features` -- all text in Bulgarian, same layout
4. Check browser tab title shows "Features | Planifactor" (EN) or "Функции | Planifactor" (BG)
5. Resize to mobile (375px) -- cards stack to single column, all content visible
6. Resize to tablet (768px) -- cards in 2-column grid
</verify>
</task>

## Verification

- [ ] 6 new shadcn/ui components installed (`card`, `badge`, `separator`, `tabs`, `accordion`, `table`)
- [ ] `messages/en.json` has `FeaturesPage` namespace with ~90 keys
- [ ] `messages/bg.json` has `FeaturesPage` namespace with ~90 keys (Bulgarian text)
- [ ] `FeatureHero.tsx` exists at `src/components/sections/features/FeatureHero.tsx`
- [ ] `FeatureDomainSection.tsx` exists at `src/components/sections/features/FeatureDomainSection.tsx`
- [ ] `src/app/[locale]/features/page.tsx` replaced with full features page (no more "Coming Soon")
- [ ] Features page renders 6 domain sections with alternating backgrounds
- [ ] Each domain has correct Lucide icon, title, description, and feature card grid
- [ ] Feature cards follow established pattern (`rounded-lg border bg-card p-6`)
- [ ] `generateMetadata` returns localized title and description
- [ ] `npm run build` succeeds with zero errors
- [ ] Both EN and BG locales render correctly
- [ ] Responsive: 1 col mobile, 2 col tablet, 3 col desktop for feature cards

## must_haves

- Features page at `/[locale]/features` replaces "Coming Soon" stub with full content
- 6 feature domains displayed: Scheduling, Resource Management, AI Optimization, Shifts/Calendar, BOM/Inventory, Shop Floor Tracking
- Each domain has a heading with Lucide icon, description, and 3-4 feature cards
- Future features (BOM, Inventory, Shop Floor) are presented as existing (no "coming soon" labels)
- All text translated in both EN and BG via `FeaturesPage` namespace
- Page has correct `<title>` and `<meta description>` via `generateMetadata`
- CTABanner appears at the bottom of the page
- Feature card grid is responsive (1/2/3 columns at mobile/tablet/desktop)
- 6 new shadcn/ui components (Card, Badge, Separator, Tabs, Accordion, Table) are installed and available
- `npm run build` passes with zero errors
