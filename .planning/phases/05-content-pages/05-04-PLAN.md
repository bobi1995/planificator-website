# 05-04-PLAN.md -- About Page

## Frontmatter

| Key | Value |
|-----|-------|
| wave | 2 |
| depends_on | 05-01 |
| files_modified | `messages/en.json`, `messages/bg.json`, `src/components/sections/about/AboutHero.tsx`, `src/components/sections/about/CompanyStory.tsx`, `src/components/sections/about/MissionValues.tsx`, `src/components/sections/about/TeamSection.tsx`, `src/app/[locale]/about/page.tsx` |
| autonomous | true |

## Objective

Build the About page with company story, mission/values section, and a team member grid. This is the simplest content page -- mostly static text and placeholder team data. The team section uses Avatar with initials fallback (established pattern from SocialProof). Values use Lucide icons. The page ends with CTABanner.

Purpose: Manufacturing buyers want to know who they are buying from. The About page builds trust through company story, mission clarity, and team visibility. It is a standard expectation for B2B SaaS sites.

Output: Fully built About page with ~55 translation keys per locale, 4 new section components.

## Tasks

<task id="1">
<title>Add AboutPage translations to both locale files</title>
<instructions>

Add an `AboutPage` namespace to both `messages/en.json` and `messages/bg.json`.

**In `messages/en.json`:**

```json
"AboutPage": {
  "metaTitle": "About Us",
  "metaDescription": "Learn about Planifactor -- the team, mission, and story behind AI-powered production scheduling for manufacturers.",
  "hero": {
    "title": "Building the Future of Production Scheduling",
    "subtitle": "We started Planifactor because we saw manufacturers struggling with the same problem: too much time spent on manual scheduling, not enough time for actual production."
  },
  "story": {
    "title": "Our Story",
    "paragraphs": {
      "0": "Planifactor was born from a simple observation: manufacturing scheduling is still done with spreadsheets and whiteboards. While every other business function has been transformed by software, production planners are stuck with tools from the 1990s.",
      "1": "We saw production managers spending days building weekly schedules. We watched as a single machine breakdown cascaded into chaos because nobody could quickly replan. We realized that AI could do in minutes what takes humans hours -- and do it better.",
      "2": "Today, Planifactor helps manufacturers replace reactive firefighting with proactive planning. Our AI optimization engine considers thousands of variables simultaneously, finding schedules that no human planner could discover on their own."
    }
  },
  "mission": {
    "title": "Our Mission & Values",
    "statement": "To give every manufacturer the planning intelligence that was once reserved for the largest enterprises.",
    "values": {
      "0": {
        "title": "Built for Practitioners",
        "description": "We design for production managers and planners -- not IT departments. If it does not make the shop floor easier, we do not build it."
      },
      "1": {
        "title": "Honest Technology",
        "description": "AI is not magic. We are transparent about what our optimizer can and cannot do. No black boxes, no overpromises."
      },
      "2": {
        "title": "Ship and Improve",
        "description": "We release early, listen to users, and iterate fast. Perfect is the enemy of useful."
      },
      "3": {
        "title": "Manufacturing First",
        "description": "We are not a generic scheduling tool. Every feature is designed specifically for production environments."
      }
    }
  },
  "team": {
    "title": "The Team",
    "subtitle": "A small team with deep expertise in manufacturing, AI, and enterprise software.",
    "members": {
      "0": {
        "name": "Alex Nikolov",
        "role": "Co-Founder & CEO",
        "bio": "15 years in manufacturing IT. Previously led digital transformation at two Bulgarian factories."
      },
      "1": {
        "name": "Viktor Stoyanov",
        "role": "Co-Founder & CTO",
        "bio": "AI/ML engineer with a background in operations research and combinatorial optimization."
      },
      "2": {
        "name": "Iva Petrova",
        "role": "Head of Product",
        "bio": "Former production planner turned product manager. Knows the pain of manual scheduling firsthand."
      },
      "3": {
        "name": "Martin Dimitrov",
        "role": "Lead Engineer",
        "bio": "Full-stack developer specialized in real-time systems and data visualization."
      }
    }
  }
}
```

**In `messages/bg.json`:**

```json
"AboutPage": {
  "metaTitle": "За Нас",
  "metaDescription": "Научете за Planifactor -- екипа, мисията и историята зад AI задвижваното производствено планиране за производители.",
  "hero": {
    "title": "Изграждаме Бъдещето на Производственото Планиране",
    "subtitle": "Създадохме Planifactor, защото видяхме производители, борещи се с един и същ проблем: прекалено много време за ръчно планиране, недостатъчно време за реално производство."
  },
  "story": {
    "title": "Нашата История",
    "paragraphs": {
      "0": "Planifactor се роди от просто наблюдение: производственото планиране все още се прави с таблици и бели дъски. Докато всяка друга бизнес функция е трансформирана от софтуер, производствените планисти са заседнали с инструменти от 90-те.",
      "1": "Видяхме производствени мениджъри, прекарващи дни в изграждане на седмични графици. Наблюдавахме как една повреда на машина се превръщаше в хаос, защото никой не можеше бързо да препланира. Осъзнахме, че AI може да направи за минути това, което на хората отнема часове -- и да го направи по-добре.",
      "2": "Днес Planifactor помага на производителите да заменят реактивното гасене на пожари с проактивно планиране. Нашият AI двигател за оптимизация отчита хиляди променливи едновременно, намирайки графици, които нито един човек планист не би могъл да открие сам."
    }
  },
  "mission": {
    "title": "Нашата Мисия и Ценности",
    "statement": "Да дадем на всеки производител планиращия интелект, който преди беше запазен за най-големите предприятия.",
    "values": {
      "0": {
        "title": "Създаден за Практици",
        "description": "Проектираме за производствени мениджъри и планисти -- не за IT отдели. Ако не прави цеха по-лесен, не го изграждаме."
      },
      "1": {
        "title": "Честна Технология",
        "description": "AI не е магия. Ние сме прозрачни за това какво нашият оптимизатор може и не може да направи. Без черни кутии, без свръхобещания."
      },
      "2": {
        "title": "Пускай и Подобрявай",
        "description": "Пускаме рано, слушаме потребителите и итерираме бързо. Перфектното е враг на полезното."
      },
      "3": {
        "title": "Производството Първо",
        "description": "Не сме общ инструмент за планиране. Всяка функция е проектирана специално за производствена среда."
      }
    }
  },
  "team": {
    "title": "Екипът",
    "subtitle": "Малък екип с дълбока експертиза в производството, AI и корпоративен софтуер.",
    "members": {
      "0": {
        "name": "Алекс Николов",
        "role": "Съосновател и CEO",
        "bio": "15 години в производствени IT. Преди ръководеше дигиталната трансформация в две български фабрики."
      },
      "1": {
        "name": "Виктор Стоянов",
        "role": "Съосновател и CTO",
        "bio": "AI/ML инженер с опит в операционни изследвания и комбинаторна оптимизация."
      },
      "2": {
        "name": "Ива Петрова",
        "role": "Ръководител Продукт",
        "bio": "Бивш производствен планист, станал продуктов мениджър. Познава болката от ръчното планиране от първа ръка."
      },
      "3": {
        "name": "Мартин Димитров",
        "role": "Водещ Инженер",
        "bio": "Full-stack разработчик, специализиран в системи в реално време и визуализация на данни."
      }
    }
  }
}
```

Verify JSON validity after adding:

```bash
node -e "JSON.parse(require('fs').readFileSync('messages/en.json','utf8'))"
node -e "JSON.parse(require('fs').readFileSync('messages/bg.json','utf8'))"
```
</instructions>
<verify>
1. Both locale files parse without errors
2. `AboutPage` namespace exists in both files with: `hero`, `story` (3 paragraphs), `mission` (statement + 4 values), `team` (4 members)
</verify>
</task>

<task id="2" depends_on="1">
<title>Create about section components and replace the About page stub</title>
<instructions>

Create the `src/components/sections/about/` directory, then create the following files:

---

**File 1: `src/components/sections/about/AboutHero.tsx`**

Async server component.

```typescript
import {getTranslations} from 'next-intl/server';

export async function AboutHero() {
  const t = await getTranslations('AboutPage');

  return (
    <section className="py-20 px-4 bg-muted/30">
      <div className="max-w-4xl mx-auto text-center">
        <h1 className="text-display md:text-hero">
          {t('hero.title')}
        </h1>
        <p className="mt-6 text-body-lg text-muted-foreground max-w-3xl mx-auto">
          {t('hero.subtitle')}
        </p>
      </div>
    </section>
  );
}
```

---

**File 2: `src/components/sections/about/CompanyStory.tsx`**

Async server component. Renders the company origin story as a multi-paragraph narrative.

```typescript
import {getTranslations} from 'next-intl/server';

export async function CompanyStory() {
  const t = await getTranslations('AboutPage.story');

  return (
    <section className="py-20 px-4">
      <div className="max-w-3xl mx-auto">
        <h2 className="text-heading md:text-display mb-8">
          {t('title')}
        </h2>
        <div className="space-y-6 text-body-lg text-muted-foreground">
          {[0, 1, 2].map((i) => (
            <p key={i}>{t(`paragraphs.${i}`)}</p>
          ))}
        </div>
      </div>
    </section>
  );
}
```

---

**File 3: `src/components/sections/about/MissionValues.tsx`**

Async server component. Renders the mission statement and 4 values with Lucide icons.

```typescript
import {getTranslations} from 'next-intl/server';
import {Wrench, Shield, Rocket, Factory} from 'lucide-react';

const valueIcons = [Wrench, Shield, Rocket, Factory];

export async function MissionValues() {
  const t = await getTranslations('AboutPage.mission');

  return (
    <section className="py-20 px-4 bg-muted/30">
      <div className="max-w-7xl mx-auto">
        <div className="text-center mb-12">
          <h2 className="text-heading md:text-display mb-4">
            {t('title')}
          </h2>
          <p className="text-xl text-foreground font-medium max-w-3xl mx-auto italic">
            &ldquo;{t('statement')}&rdquo;
          </p>
        </div>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
          {[0, 1, 2, 3].map((i) => {
            const Icon = valueIcons[i];
            return (
              <div key={i} className="rounded-lg border bg-card p-6">
                <Icon className="h-8 w-8 text-brand-600 mb-3" />
                <h3 className="text-subheading mb-2">
                  {t(`values.${i}.title`)}
                </h3>
                <p className="text-muted-foreground">
                  {t(`values.${i}.description`)}
                </p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
}
```

---

**File 4: `src/components/sections/about/TeamSection.tsx`**

Async server component. Renders a grid of team member cards with Avatar initials (established pattern from SocialProof).

```typescript
import {getTranslations} from 'next-intl/server';
import {Avatar, AvatarFallback} from '@/components/ui/avatar';

export async function TeamSection() {
  const t = await getTranslations('AboutPage.team');

  return (
    <section className="py-20 px-4">
      <div className="max-w-7xl mx-auto">
        <div className="text-center mb-12">
          <h2 className="text-heading md:text-display">
            {t('title')}
          </h2>
          <p className="mt-4 text-body-lg text-muted-foreground max-w-2xl mx-auto">
            {t('subtitle')}
          </p>
        </div>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
          {[0, 1, 2, 3].map((i) => (
            <div key={i} className="text-center">
              <Avatar className="h-20 w-20 mx-auto mb-4">
                <AvatarFallback className="bg-brand-100 text-brand-700 text-xl font-semibold">
                  {t(`members.${i}.name`)
                    .split(' ')
                    .map((n: string) => n[0])
                    .join('')}
                </AvatarFallback>
              </Avatar>
              <h3 className="font-semibold text-lg">
                {t(`members.${i}.name`)}
              </h3>
              <p className="text-brand-600 text-sm font-medium mb-2">
                {t(`members.${i}.role`)}
              </p>
              <p className="text-muted-foreground text-sm">
                {t(`members.${i}.bio`)}
              </p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
```

Notes:
- Avatar uses `h-20 w-20` for a larger display than the SocialProof cards
- Initials are extracted with the same `.split(' ').map(n => n[0]).join('')` pattern
- This works for both Latin (EN) and Cyrillic (BG) characters (established in Phase 3)
- Roles are displayed in `text-brand-600` for visual hierarchy

---

**File 5: Replace `src/app/[locale]/about/page.tsx`**

Replace the entire stub with the full About page:

```typescript
import {setRequestLocale, getTranslations} from 'next-intl/server';
import {AboutHero} from '@/components/sections/about/AboutHero';
import {CompanyStory} from '@/components/sections/about/CompanyStory';
import {MissionValues} from '@/components/sections/about/MissionValues';
import {TeamSection} from '@/components/sections/about/TeamSection';
import {CTABanner} from '@/components/sections/CTABanner';

type Props = {
  params: Promise<{locale: string}>;
};

export async function generateMetadata({params}: Props) {
  const {locale} = await params;
  const t = await getTranslations({locale, namespace: 'AboutPage'});
  return {
    title: `${t('metaTitle')} | Planifactor`,
    description: t('metaDescription'),
  };
}

export default async function AboutPage({params}: Props) {
  const {locale} = await params;
  setRequestLocale(locale);

  return (
    <>
      <AboutHero />
      <CompanyStory />
      <MissionValues />
      <TeamSection />
      <CTABanner />
    </>
  );
}
```

Section flow: Hero (bg-muted/30) -> Story (white) -> Mission/Values (bg-muted/30) -> Team (white) -> CTABanner (bg-brand-600). Alternating backgrounds create visual rhythm.
</instructions>
<verify>
1. Run `npm run build` -- must succeed with zero errors
2. Run `npm run dev` and visit `http://localhost:3000/en/about`:
   - Hero with "Building the Future of Production Scheduling" heading
   - Company Story section with 3 paragraphs of narrative
   - Mission & Values section with quoted mission statement and 4 value cards with icons
   - Team section with 4 member cards (Avatar initials, name, role, bio)
   - CTABanner at the bottom
3. Visit `http://localhost:3000/bg/about` -- all text in Bulgarian, avatars show Cyrillic initials
4. Browser tab shows "About Us | Planifactor" (EN) or "За Нас | Planifactor" (BG)
5. Resize to mobile (375px):
   - All sections stack to single column
   - Team member cards stack vertically
   - Value cards stack vertically
6. Resize to tablet (768px):
   - Value cards in 2-column grid
   - Team cards in 2-column grid
7. Resize to desktop (1024px+):
   - Team cards in 4-column grid
</verify>
</task>

## Verification

- [ ] `messages/en.json` has `AboutPage` namespace (~55 keys)
- [ ] `messages/bg.json` has `AboutPage` namespace (~55 keys, Bulgarian)
- [ ] `AboutHero.tsx` exists at `src/components/sections/about/AboutHero.tsx`
- [ ] `CompanyStory.tsx` exists at `src/components/sections/about/CompanyStory.tsx`
- [ ] `MissionValues.tsx` exists at `src/components/sections/about/MissionValues.tsx`
- [ ] `TeamSection.tsx` exists at `src/components/sections/about/TeamSection.tsx`
- [ ] `src/app/[locale]/about/page.tsx` replaced with full about page (no more "Coming Soon")
- [ ] Company story renders 3 paragraphs of narrative text
- [ ] Mission statement displayed as an italic quote
- [ ] 4 values displayed with Lucide icons in a 2-column grid
- [ ] 4 team members displayed with Avatar initials, name, role, and bio
- [ ] Cyrillic initials display correctly on BG locale
- [ ] `generateMetadata` returns localized title and description
- [ ] `npm run build` succeeds with zero errors
- [ ] Both EN and BG locales render correctly
- [ ] Responsive: single column mobile, 2-col tablet for values, 4-col desktop for team

## must_haves

- About page at `/[locale]/about` replaces "Coming Soon" stub with full content
- Company story section with 3 paragraphs of origin narrative
- Mission statement displayed prominently as a quoted statement
- 4 core values with Lucide icons in a responsive grid
- Team section with 4 placeholder members showing Avatar initials, name, role, and bio
- Avatar initials work correctly for both Latin (EN) and Cyrillic (BG) names
- All text translated in both EN and BG via `AboutPage` namespace
- Page has correct `<title>` and `<meta description>` via `generateMetadata`
- CTABanner appears at the bottom of the page
- `npm run build` passes with zero errors
