# 07-01-PLAN.md -- ROI Calculator

## Frontmatter

| Key | Value |
|-----|-------|
| wave | 1 |
| depends_on | (none) |
| files_modified | `src/components/ui/label.tsx`, `src/components/ui/input.tsx`, `src/components/ui/select.tsx`, `src/lib/roi-calculator.ts`, `src/components/interactive/ROICalculator.tsx`, `src/app/[locale]/roi-calculator/page.tsx`, `messages/en.json`, `messages/bg.json` |
| autonomous | true |

## Objective

Build the ROI Calculator -- a standalone page at `/[locale]/roi-calculator` where manufacturing decision-makers input 4 production metrics and see estimated time/cost savings from using Planifactor. The calculator uses conservative methodology (ranges, not exact numbers; capped values; visible methodology section) to build trust rather than hype. Pure calculation logic lives in a separate utility file; the React component handles form state and display.

Purpose: This is a self-qualification tool that converts curious visitors into informed leads. A visitor who sees "you could save 8-12 hours/week" is far more likely to request a demo than one who just reads marketing copy.

Output: 3 new shadcn components (Label, Input, Select), 1 pure calculation utility, 1 client component, 1 new page, ~35 new translation keys per locale.

## Tasks

<task id="1">
<title>Install shadcn components and create ROI calculation logic</title>
<instructions>
**Step 1: Install 3 shadcn/ui components.**

Run these commands from the project root:

```bash
npx shadcn@latest add label
npx shadcn@latest add input
npx shadcn@latest add select
```

Each command creates a file in `src/components/ui/`. Verify all 3 new files exist after installation.

**Step 2: Create the pure calculation utility at `src/lib/roi-calculator.ts`**

This file contains zero React code. It exports types and a pure function that takes inputs and returns results. This separation enables easy testing and reuse.

```typescript
export type SchedulingMethod = 'excel' | 'whiteboard' | 'software' | 'none';

export interface ROIInputs {
  ordersPerWeek: number;
  schedulingHoursPerWeek: number;
  numberOfResources: number;
  currentMethod: SchedulingMethod;
}

export interface ROIResults {
  hoursSavedPerWeekMin: number;
  hoursSavedPerWeekMax: number;
  hoursSavedPerYearMin: number;
  hoursSavedPerYearMax: number;
  costSavingsPerYearMin: number;
  costSavingsPerYearMax: number;
  speedImprovementMin: number;
  speedImprovementMax: number;
}

// Efficiency gain ranges by current scheduling method
const EFFICIENCY_GAINS: Record<SchedulingMethod, [number, number]> = {
  excel: [0.60, 0.75],
  whiteboard: [0.70, 0.85],
  software: [0.40, 0.55],
  none: [0.75, 0.90],
};

// Complexity modifier based on number of production resources
function getComplexityModifier(resources: number): number {
  if (resources <= 10) return 0.85;
  if (resources <= 50) return 1.0;
  return 1.1; // 51+
}

// Volume modifier based on orders per week
function getVolumeModifier(orders: number): number {
  if (orders <= 50) return 0.9;
  if (orders <= 150) return 1.0;
  if (orders <= 300) return 1.05;
  return 1.1; // 300+
}

const WORKING_WEEKS_PER_YEAR = 50;
const DEFAULT_HOURLY_RATE_EUR = 35;

/**
 * Calculate estimated ROI from switching to Planifactor.
 *
 * Design principles:
 * - Returns RANGES, not single numbers
 * - Caps weekly savings at 80% of input hours (never claim 100% elimination)
 * - Caps speed improvement at 10x
 * - Uses conservative multipliers throughout
 */
export function calculateROI(inputs: ROIInputs): ROIResults {
  const { ordersPerWeek, schedulingHoursPerWeek, numberOfResources, currentMethod } = inputs;

  const [gainMin, gainMax] = EFFICIENCY_GAINS[currentMethod];
  const complexityMod = getComplexityModifier(numberOfResources);
  const volumeMod = getVolumeModifier(ordersPerWeek);

  // Raw hours saved per week
  let rawMin = schedulingHoursPerWeek * gainMin * complexityMod * volumeMod;
  let rawMax = schedulingHoursPerWeek * gainMax * complexityMod * volumeMod;

  // Cap at 80% of input hours (never claim total elimination)
  const cap = schedulingHoursPerWeek * 0.8;
  rawMin = Math.min(rawMin, cap);
  rawMax = Math.min(rawMax, cap);

  // Round to 1 decimal
  const hoursSavedPerWeekMin = Math.round(rawMin * 10) / 10;
  const hoursSavedPerWeekMax = Math.round(rawMax * 10) / 10;

  // Annual projection (50 working weeks)
  const hoursSavedPerYearMin = Math.round(hoursSavedPerWeekMin * WORKING_WEEKS_PER_YEAR);
  const hoursSavedPerYearMax = Math.round(hoursSavedPerWeekMax * WORKING_WEEKS_PER_YEAR);

  // Cost savings
  const costSavingsPerYearMin = hoursSavedPerYearMin * DEFAULT_HOURLY_RATE_EUR;
  const costSavingsPerYearMax = hoursSavedPerYearMax * DEFAULT_HOURLY_RATE_EUR;

  // Speed improvement factor: how many times faster
  // If you saved 75% of time, you're ~4x faster (1 / (1 - 0.75))
  // Cap at 10x
  const effectiveGainMin = Math.min(rawMin / schedulingHoursPerWeek, 0.8);
  const effectiveGainMax = Math.min(rawMax / schedulingHoursPerWeek, 0.8);
  const speedImprovementMin = Math.min(
    Math.round((1 / (1 - effectiveGainMin)) * 10) / 10,
    10
  );
  const speedImprovementMax = Math.min(
    Math.round((1 / (1 - effectiveGainMax)) * 10) / 10,
    10
  );

  return {
    hoursSavedPerWeekMin,
    hoursSavedPerWeekMax,
    hoursSavedPerYearMin,
    hoursSavedPerYearMax,
    costSavingsPerYearMin,
    costSavingsPerYearMax,
    speedImprovementMin,
    speedImprovementMax,
  };
}
```

Key implementation details:
- `EFFICIENCY_GAINS` uses ranges (min, max) per scheduling method
- `getComplexityModifier` and `getVolumeModifier` are step functions based on thresholds
- The 80% cap prevents absurd claims (e.g., saving 38 out of 40 hours)
- Speed improvement capped at 10x
- `DEFAULT_HOURLY_RATE_EUR = 35` for cost projection (average production planner rate in EU)
- No React imports -- pure TypeScript utility

**Step 3: Test the calculation with extreme inputs**

After creating the file, run a quick Node.js sanity check:

```bash
npx tsx -e "
const { calculateROI } = require('./src/lib/roi-calculator');
// Test 1: Minimal inputs
const r1 = calculateROI({ ordersPerWeek: 10, schedulingHoursPerWeek: 1, numberOfResources: 5, currentMethod: 'excel' });
console.log('Minimal:', JSON.stringify(r1));
// Test 2: Maximal inputs
const r2 = calculateROI({ ordersPerWeek: 500, schedulingHoursPerWeek: 40, numberOfResources: 100, currentMethod: 'whiteboard' });
console.log('Maximal:', JSON.stringify(r2));
// Verify caps: weekly savings should never exceed 80% of input
console.log('Cap check (max):', r2.hoursSavedPerWeekMax <= 32 ? 'PASS' : 'FAIL');
console.log('Speed check (max):', r2.speedImprovementMax <= 10 ? 'PASS' : 'FAIL');
"
```

Note: If `npx tsx` does not work (tsx may not be installed), the executor can verify the logic by running `npm run build` later -- TypeScript compilation will catch any type errors. The important thing is that the logic file exists and is well-typed.
</instructions>
</task>

<task id="2" depends_on="1">
<title>Create ROICalculator component, page, and translations</title>
<instructions>
**Step 1: Add `ROICalculator` namespace to `messages/en.json`**

Add the following namespace to the EN translation file. Place it after the last existing namespace, before the closing brace:

```json
"ROICalculator": {
  "metaTitle": "ROI Calculator",
  "metaDescription": "Calculate how much time and money Planifactor could save your production planning team. Enter your details and see estimated savings.",
  "hero": {
    "title": "How Much Time Could You Save?",
    "subtitle": "Enter your production details below and see how AI-powered scheduling could transform your planning process."
  },
  "inputs": {
    "ordersPerWeek": "Orders per Week",
    "ordersPerWeekHelp": "Average number of production orders you schedule each week",
    "schedulingHours": "Scheduling Time (hours/week)",
    "schedulingHoursHelp": "Hours your team currently spends on production scheduling per week",
    "resources": "Number of Resources",
    "resourcesHelp": "Total machines, workers, or workstations you schedule across",
    "currentMethod": "Current Scheduling Method",
    "currentMethodHelp": "How do you currently create your production schedule?",
    "methods": {
      "excel": "Excel / Spreadsheet",
      "whiteboard": "Whiteboard / Paper",
      "software": "Other Software",
      "none": "No Formal Method"
    }
  },
  "calculate": "Calculate Savings",
  "reset": "Reset",
  "results": {
    "title": "Your Estimated Savings",
    "hoursSavedWeek": "Hours Saved Per Week",
    "hoursSavedYear": "Hours Saved Per Year",
    "costSavingsYear": "Estimated Annual Cost Savings",
    "speedImprovement": "Scheduling Speed Improvement",
    "fasterSuffix": "faster",
    "perWeek": "per week",
    "perYear": "per year",
    "disclaimer": "Estimates based on industry benchmarks. Actual results vary based on your specific environment."
  },
  "methodology": {
    "title": "How We Calculated This",
    "description": "Our estimates are based on observed efficiency gains across manufacturing environments of similar size and complexity.",
    "conservative": "We use conservative estimates and show ranges rather than exact numbers. The lower bound represents a cautious projection; the upper bound represents typical best-case outcomes.",
    "factors": "Factors considered: your current scheduling method, number of production resources, and weekly order volume. Cost savings assume an average production planner rate of EUR 35/hour."
  },
  "cta": {
    "title": "Want to Validate These Numbers?",
    "description": "Book a free demo and we'll run the analysis with your real production data -- no commitment required.",
    "button": "Book a Demo"
  }
}
```

**Step 2: Add `ROICalculator` namespace to `messages/bg.json`**

Add the Bulgarian translation:

```json
"ROICalculator": {
  "metaTitle": "ROI Калкулатор",
  "metaDescription": "Изчислете колко време и пари Planifactor може да спести на вашия екип за производствено планиране. Въведете данните си и вижте очакваните спестявания.",
  "hero": {
    "title": "Колко време бихте могли да спестите?",
    "subtitle": "Въведете данните за вашето производство и вижте как AI планирането може да трансформира процеса ви."
  },
  "inputs": {
    "ordersPerWeek": "Поръчки на седмица",
    "ordersPerWeekHelp": "Среден брой производствени поръчки, които планирате всяка седмица",
    "schedulingHours": "Време за планиране (часове/седмица)",
    "schedulingHoursHelp": "Часове, които екипът ви изразходва за производствено планиране на седмица",
    "resources": "Брой ресурси",
    "resourcesHelp": "Общ брой машини, работници или работни станции, които планирате",
    "currentMethod": "Текущ метод за планиране",
    "currentMethodHelp": "Как създавате в момента вашия производствен график?",
    "methods": {
      "excel": "Excel / Електронна таблица",
      "whiteboard": "Бяла дъска / Хартия",
      "software": "Друг софтуер",
      "none": "Без формален метод"
    }
  },
  "calculate": "Изчисли спестяванията",
  "reset": "Нулиране",
  "results": {
    "title": "Вашите очаквани спестявания",
    "hoursSavedWeek": "Спестени часове на седмица",
    "hoursSavedYear": "Спестени часове на година",
    "costSavingsYear": "Очаквани годишни спестявания",
    "speedImprovement": "Подобрение на скоростта на планиране",
    "fasterSuffix": "по-бързо",
    "perWeek": "на седмица",
    "perYear": "на година",
    "disclaimer": "Оценки, базирани на индустриални показатели. Реалните резултати варират спрямо вашата конкретна среда."
  },
  "methodology": {
    "title": "Как изчислихме това",
    "description": "Нашите оценки се базират на наблюдавани подобрения в ефективността в производствени среди с подобен мащаб и сложност.",
    "conservative": "Използваме консервативни оценки и показваме диапазони вместо точни числа. Долната граница представлява предпазлива прогноза; горната граница представлява типичен оптимален сценарий.",
    "factors": "Разгледани фактори: текущ метод за планиране, брой производствени ресурси и седмичен обем на поръчките. Спестяванията предполагат средна ставка на производствен планьор от 35 EUR/час."
  },
  "cta": {
    "title": "Искате да потвърдите тези числа?",
    "description": "Запазете безплатна демонстрация и ще направим анализ с вашите реални производствени данни -- без ангажимент.",
    "button": "Запазете демо"
  }
}
```

After adding both namespaces, verify JSON validity:
```bash
node -e "JSON.parse(require('fs').readFileSync('messages/en.json','utf8'))"
node -e "JSON.parse(require('fs').readFileSync('messages/bg.json','utf8'))"
```

**Step 3: Create ROICalculator client component at `src/components/interactive/ROICalculator.tsx`**

This is a `'use client'` component that manages form state, calls the pure calculation function, and displays results.

```typescript
'use client';

import {useState} from 'react';
import {useTranslations} from 'next-intl';
import {Button} from '@/components/ui/button';
import {Input} from '@/components/ui/input';
import {Label} from '@/components/ui/label';
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from '@/components/ui/select';
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from '@/components/ui/accordion';
import {Card, CardContent, CardHeader, CardTitle} from '@/components/ui/card';
import {Link} from '@/i18n/navigation';
import {calculateROI, type SchedulingMethod, type ROIResults} from '@/lib/roi-calculator';
import {Clock, TrendingUp, DollarSign, Zap} from 'lucide-react';

export function ROICalculator() {
  const t = useTranslations('ROICalculator');

  const [ordersPerWeek, setOrdersPerWeek] = useState<string>('');
  const [schedulingHours, setSchedulingHours] = useState<string>('');
  const [resources, setResources] = useState<string>('');
  const [currentMethod, setCurrentMethod] = useState<SchedulingMethod | ''>('');
  const [results, setResults] = useState<ROIResults | null>(null);

  const isValid =
    ordersPerWeek !== '' &&
    schedulingHours !== '' &&
    resources !== '' &&
    currentMethod !== '' &&
    Number(ordersPerWeek) > 0 &&
    Number(schedulingHours) > 0 &&
    Number(resources) > 0;

  function handleCalculate() {
    if (!isValid || currentMethod === '') return;
    const roi = calculateROI({
      ordersPerWeek: Number(ordersPerWeek),
      schedulingHoursPerWeek: Number(schedulingHours),
      numberOfResources: Number(resources),
      currentMethod,
    });
    setResults(roi);
  }

  function handleReset() {
    setOrdersPerWeek('');
    setSchedulingHours('');
    setResources('');
    setCurrentMethod('');
    setResults(null);
  }

  const methods: SchedulingMethod[] = ['excel', 'whiteboard', 'software', 'none'];

  return (
    <div className="max-w-4xl mx-auto">
      {/* Input form */}
      <Card>
        <CardContent className="pt-6">
          <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
            {/* Orders per week */}
            <div className="space-y-2">
              <Label htmlFor="ordersPerWeek">{t('inputs.ordersPerWeek')}</Label>
              <Input
                id="ordersPerWeek"
                type="number"
                min={1}
                max={9999}
                placeholder="e.g. 50"
                value={ordersPerWeek}
                onChange={(e) => setOrdersPerWeek(e.target.value)}
              />
              <p className="text-sm text-muted-foreground">{t('inputs.ordersPerWeekHelp')}</p>
            </div>

            {/* Scheduling hours */}
            <div className="space-y-2">
              <Label htmlFor="schedulingHours">{t('inputs.schedulingHours')}</Label>
              <Input
                id="schedulingHours"
                type="number"
                min={0.5}
                max={80}
                step={0.5}
                placeholder="e.g. 10"
                value={schedulingHours}
                onChange={(e) => setSchedulingHours(e.target.value)}
              />
              <p className="text-sm text-muted-foreground">{t('inputs.schedulingHoursHelp')}</p>
            </div>

            {/* Number of resources */}
            <div className="space-y-2">
              <Label htmlFor="resources">{t('inputs.resources')}</Label>
              <Input
                id="resources"
                type="number"
                min={1}
                max={9999}
                placeholder="e.g. 20"
                value={resources}
                onChange={(e) => setResources(e.target.value)}
              />
              <p className="text-sm text-muted-foreground">{t('inputs.resourcesHelp')}</p>
            </div>

            {/* Current method */}
            <div className="space-y-2">
              <Label htmlFor="currentMethod">{t('inputs.currentMethod')}</Label>
              <Select
                value={currentMethod}
                onValueChange={(val) => setCurrentMethod(val as SchedulingMethod)}
              >
                <SelectTrigger id="currentMethod">
                  <SelectValue placeholder="---" />
                </SelectTrigger>
                <SelectContent>
                  {methods.map((method) => (
                    <SelectItem key={method} value={method}>
                      {t(`inputs.methods.${method}`)}
                    </SelectItem>
                  ))}
                </SelectContent>
              </Select>
              <p className="text-sm text-muted-foreground">{t('inputs.currentMethodHelp')}</p>
            </div>
          </div>

          <div className="mt-8 flex gap-3">
            <Button onClick={handleCalculate} disabled={!isValid} size="lg">
              {t('calculate')}
            </Button>
            {results && (
              <Button onClick={handleReset} variant="outline" size="lg">
                {t('reset')}
              </Button>
            )}
          </div>
        </CardContent>
      </Card>

      {/* Results */}
      {results && (
        <div className="mt-8 space-y-8">
          <h2 className="text-heading text-center">{t('results.title')}</h2>

          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
            {/* Hours saved per week */}
            <Card className="text-center">
              <CardContent className="pt-6">
                <Clock className="h-8 w-8 text-brand-600 mx-auto mb-2" />
                <p className="text-sm text-muted-foreground mb-1">{t('results.hoursSavedWeek')}</p>
                <p className="text-display font-bold text-brand-600">
                  {results.hoursSavedPerWeekMin}-{results.hoursSavedPerWeekMax}
                </p>
                <p className="text-sm text-muted-foreground">{t('results.perWeek')}</p>
              </CardContent>
            </Card>

            {/* Hours saved per year */}
            <Card className="text-center">
              <CardContent className="pt-6">
                <TrendingUp className="h-8 w-8 text-brand-600 mx-auto mb-2" />
                <p className="text-sm text-muted-foreground mb-1">{t('results.hoursSavedYear')}</p>
                <p className="text-display font-bold text-brand-600">
                  {results.hoursSavedPerYearMin}-{results.hoursSavedPerYearMax}
                </p>
                <p className="text-sm text-muted-foreground">{t('results.perYear')}</p>
              </CardContent>
            </Card>

            {/* Cost savings */}
            <Card className="text-center">
              <CardContent className="pt-6">
                <DollarSign className="h-8 w-8 text-brand-600 mx-auto mb-2" />
                <p className="text-sm text-muted-foreground mb-1">{t('results.costSavingsYear')}</p>
                <p className="text-display font-bold text-brand-600">
                  EUR {results.costSavingsPerYearMin.toLocaleString()}-{results.costSavingsPerYearMax.toLocaleString()}
                </p>
                <p className="text-sm text-muted-foreground">{t('results.perYear')}</p>
              </CardContent>
            </Card>

            {/* Speed improvement */}
            <Card className="text-center">
              <CardContent className="pt-6">
                <Zap className="h-8 w-8 text-brand-600 mx-auto mb-2" />
                <p className="text-sm text-muted-foreground mb-1">{t('results.speedImprovement')}</p>
                <p className="text-display font-bold text-brand-600">
                  {results.speedImprovementMin}-{results.speedImprovementMax}x
                </p>
                <p className="text-sm text-muted-foreground">{t('results.fasterSuffix')}</p>
              </CardContent>
            </Card>
          </div>

          <p className="text-sm text-muted-foreground text-center italic">
            {t('results.disclaimer')}
          </p>

          {/* Methodology accordion */}
          <Accordion type="single" collapsible className="max-w-2xl mx-auto">
            <AccordionItem value="methodology">
              <AccordionTrigger>{t('methodology.title')}</AccordionTrigger>
              <AccordionContent className="space-y-3 text-muted-foreground">
                <p>{t('methodology.description')}</p>
                <p>{t('methodology.conservative')}</p>
                <p>{t('methodology.factors')}</p>
              </AccordionContent>
            </AccordionItem>
          </Accordion>

          {/* CTA */}
          <Card className="bg-brand-50 border-brand-200">
            <CardContent className="pt-6 text-center">
              <h3 className="text-subheading mb-2">{t('cta.title')}</h3>
              <p className="text-muted-foreground mb-4 max-w-lg mx-auto">{t('cta.description')}</p>
              <Button asChild size="lg">
                <Link href="/contact">{t('cta.button')}</Link>
              </Button>
            </CardContent>
          </Card>
        </div>
      )}
    </div>
  );
}
```

Key implementation details:
- `'use client'` directive -- form state requires client-side interactivity
- Uses `useTranslations` (client hook, correct for client components)
- Imports `calculateROI` from the pure utility (no calculation logic in the component)
- Form validation: all 4 fields must be non-empty and positive
- Results displayed in a 4-card grid (responsive: 1 col mobile, 2 col sm, 4 col lg)
- Each result card shows icon, label, range value, and unit
- Methodology in an Accordion (collapsed by default, user can expand)
- Soft CTA at the bottom links to `/contact` (not gated behind email capture)
- Uses `Link` from `@/i18n/navigation` for locale-aware routing
- All number displays use ranges ("8-12" not "10.3")

**Step 4: Create the ROI Calculator page at `src/app/[locale]/roi-calculator/page.tsx`**

Create the directory `src/app/[locale]/roi-calculator/` first, then create page.tsx:

```typescript
import {setRequestLocale, getTranslations} from 'next-intl/server';
import {ROICalculator} from '@/components/interactive/ROICalculator';
import {CTABanner} from '@/components/sections/CTABanner';

type Props = {
  params: Promise<{locale: string}>;
};

export async function generateMetadata({params}: Props) {
  const {locale} = await params;
  const t = await getTranslations({locale, namespace: 'ROICalculator'});
  return {
    title: `${t('metaTitle')} | Planifactor`,
    description: t('metaDescription'),
  };
}

export default async function ROICalculatorPage({params}: Props) {
  const {locale} = await params;
  setRequestLocale(locale);
  const t = await getTranslations({locale, namespace: 'ROICalculator'});

  return (
    <>
      <section className="py-20 px-4 bg-muted/30">
        <div className="max-w-4xl mx-auto text-center">
          <h1 className="text-display md:text-hero">{t('hero.title')}</h1>
          <p className="mt-6 text-body-lg text-muted-foreground max-w-3xl mx-auto">
            {t('hero.subtitle')}
          </p>
        </div>
      </section>

      <section className="py-16 px-4">
        <ROICalculator />
      </section>

      <CTABanner />
    </>
  );
}
```

Key details:
- Server component page -- composes the client ROICalculator within a server shell
- Hero section follows established pattern: `text-display md:text-hero` for H1, `bg-muted/30` background
- `generateMetadata` provides localized title and description for SEO
- `setRequestLocale` required for static rendering with next-intl
- CTABanner reused at bottom (consistent with all content pages)
- The hero text comes from server `getTranslations`, the calculator uses client `useTranslations`
</instructions>
</task>

## Verification

<verification>
Run the following commands in order to verify the ROI calculator is correctly built:

```bash
# 1. Verify shadcn components installed
test -f src/components/ui/label.tsx && test -f src/components/ui/input.tsx && test -f src/components/ui/select.tsx && echo "PASS: shadcn components" || echo "FAIL: shadcn components missing"

# 2. Verify ROI calculation utility exists with correct exports
node -e "
const fs = require('fs');
const src = fs.readFileSync('src/lib/roi-calculator.ts', 'utf-8');
const required = ['calculateROI', 'ROIInputs', 'ROIResults', 'SchedulingMethod'];
const missing = required.filter(fn => !src.includes(fn));
if (missing.length) { console.log('FAIL: missing exports:', missing); process.exit(1); }
console.log('PASS: all roi-calculator exports found');
"

# 3. Verify ROICalculator component exists and is client component
grep -q "'use client'" src/components/interactive/ROICalculator.tsx && echo "PASS: ROICalculator is client component" || echo "FAIL: missing use client"

# 4. Verify ROI calculator page exists
test -f src/app/\[locale\]/roi-calculator/page.tsx && echo "PASS: ROI calculator page" || echo "FAIL: page missing"

# 5. Verify translation namespaces
node -e "
const en = JSON.parse(require('fs').readFileSync('messages/en.json','utf8'));
const bg = JSON.parse(require('fs').readFileSync('messages/bg.json','utf8'));
if (!en.ROICalculator) { console.log('FAIL: EN missing ROICalculator namespace'); process.exit(1); }
if (!bg.ROICalculator) { console.log('FAIL: BG missing ROICalculator namespace'); process.exit(1); }
if (!en.ROICalculator.inputs || !en.ROICalculator.results || !en.ROICalculator.methodology) { console.log('FAIL: EN missing sub-namespaces'); process.exit(1); }
if (!bg.ROICalculator.inputs || !bg.ROICalculator.results || !bg.ROICalculator.methodology) { console.log('FAIL: BG missing sub-namespaces'); process.exit(1); }
console.log('PASS: translation namespaces complete');
"

# 6. Full build
npm run build
```

All checks must pass. The build must complete with zero errors. After build, navigate to `/en/roi-calculator` to verify the page renders.
</verification>

## must_haves

1. ROI calculator page exists at `/[locale]/roi-calculator` and renders in both EN and BG
2. Calculator accepts 4 inputs: orders/week (number), scheduling hours/week (number), number of resources (number), current method (dropdown with 4 options)
3. Results show RANGES for all 4 metrics: hours saved/week, hours saved/year, cost savings/year (EUR), speed improvement factor
4. Weekly hours saved is capped at 80% of input hours; speed improvement is capped at 10x
5. Methodology section is expandable (Accordion) and explains the calculation approach with conservative framing
6. Soft CTA after results links to `/contact` for demo booking
7. Pure calculation logic in `src/lib/roi-calculator.ts` is separated from React component
8. `ROICalculator` namespace exists in both `messages/en.json` and `messages/bg.json` with ~35 keys each
9. 3 shadcn components installed: Label, Input, Select
10. `npm run build` passes with zero errors
