# 06-02-PLAN.md -- Blog Pages, UI Components, Translations & RSS

## Frontmatter

| Key | Value |
|-----|-------|
| wave | 2 |
| depends_on | 06-01 |
| files_modified | `messages/en.json`, `messages/bg.json`, `src/components/sections/blog/BlogHero.tsx`, `src/components/sections/blog/PostCard.tsx`, `src/components/sections/blog/PostHeader.tsx`, `src/components/sections/blog/TagFilter.tsx`, `src/components/sections/blog/BlogPagination.tsx`, `src/app/[locale]/blog/page.tsx`, `src/app/[locale]/blog/[slug]/page.tsx`, `src/app/feed.xml/route.ts` |
| autonomous | true |

## Objective

Build all blog UI components, pages, and the RSS feed. This plan replaces the "Coming Soon" blog stub with a full blog index page listing posts with tag filtering and pagination (`?page=N`, 9 posts per page), creates the individual blog post page that renders MDX content with `next-mdx-remote-client/rsc`, adds BlogPage translations to both locale files, and creates the RSS route handler. After this plan, the blog is fully operational -- visitors can browse posts, filter by tag, paginate through results, read individual posts with proper typography, and subscribe via RSS.

## Tasks

<task id="1">
<title>Add BlogPage translations and create blog UI components</title>
<instructions>
**Step 1: Add BlogPage namespace to `messages/en.json`**

Add the following `BlogPage` namespace to the end of `messages/en.json` (before the final closing `}`). Keep all existing namespaces unchanged.

```json
"BlogPage": {
  "metaTitle": "Blog",
  "metaDescription": "Insights and guides on production scheduling, AI in manufacturing, and production optimization.",
  "heroTitle": "Blog & Resources",
  "heroSubtitle": "Insights on production scheduling, AI optimization, and smart manufacturing from the Planifactor team.",
  "allPosts": "All Posts",
  "filterByTag": "Filter by tag",
  "readMore": "Read more",
  "minRead": "min read",
  "noPosts": "No posts found",
  "noPostsForTag": "No posts found for this tag.",
  "backToBlog": "Back to Blog",
  "publishedOn": "Published on",
  "taggedWith": "Tagged with",
  "writtenBy": "By",
  "previousPage": "Previous",
  "nextPage": "Next",
  "pageOf": "Page {current} of {total}"
}
```

**Step 2: Add BlogPage namespace to `messages/bg.json`**

Add the corresponding Bulgarian translations. Keep all existing namespaces unchanged.

```json
"BlogPage": {
  "metaTitle": "Блог",
  "metaDescription": "Статии и ръководства за производствено планиране, AI в производството и оптимизация на производството.",
  "heroTitle": "Блог и ресурси",
  "heroSubtitle": "Статии за производствено планиране, AI оптимизация и интелигентно производство от екипа на Planifactor.",
  "allPosts": "Всички публикации",
  "filterByTag": "Филтрирай по таг",
  "readMore": "Прочети повече",
  "minRead": "мин четене",
  "noPosts": "Няма намерени публикации",
  "noPostsForTag": "Няма публикации с този таг.",
  "backToBlog": "Обратно към блога",
  "publishedOn": "Публикувано на",
  "taggedWith": "Тагове",
  "writtenBy": "От",
  "previousPage": "Предишна",
  "nextPage": "Следваща",
  "pageOf": "Страница {current} от {total}"
}
```

**Step 3: Create `src/components/sections/blog/BlogHero.tsx`**

Async server component matching the established hero pattern from other pages.

```typescript
import {getTranslations} from 'next-intl/server';
import {BookOpen} from 'lucide-react';

export async function BlogHero() {
  const t = await getTranslations('BlogPage');

  return (
    <section className="py-20 px-4 bg-muted/30">
      <div className="max-w-4xl mx-auto text-center">
        <BookOpen className="h-16 w-16 text-brand-600 mx-auto mb-6" />
        <h1 className="text-display md:text-hero">
          {t('heroTitle')}
        </h1>
        <p className="mt-6 text-body-lg text-muted-foreground max-w-3xl mx-auto">
          {t('heroSubtitle')}
        </p>
      </div>
    </section>
  );
}
```

Pattern follows `UseCaseHero`, `FeatureHero`, `AboutHero`, `PricingHero` -- all are async server components using `getTranslations`, with `text-display md:text-hero` for H1 and Lucide icon.

**Step 4: Create `src/components/sections/blog/PostCard.tsx`**

Server component for blog index. Displays post title, description, date, tags, reading time.

```typescript
import {Link} from '@/i18n/navigation';
import {Badge} from '@/components/ui/badge';
import {Card, CardContent, CardHeader} from '@/components/ui/card';
import {getReadingTime} from '@/lib/blog';
import type {BlogPostMeta} from '@/lib/blog';
import {useLocale} from 'next-intl';

interface PostCardProps {
  slug: string;
  meta: BlogPostMeta;
  content: string;
  readMoreLabel: string;
  minReadLabel: string;
}

export function PostCard({slug, meta, content, readMoreLabel, minReadLabel}: PostCardProps) {
  const locale = useLocale();
  const readingTime = getReadingTime(content);

  // Format date using locale-aware Intl.DateTimeFormat
  const formattedDate = new Intl.DateTimeFormat(locale, {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  }).format(new Date(meta.date));

  return (
    <Card className="flex flex-col h-full hover:shadow-lg transition-shadow">
      <CardHeader>
        <div className="flex items-center gap-2 text-sm text-muted-foreground mb-2">
          <time dateTime={meta.date}>{formattedDate}</time>
          <span aria-hidden="true">&middot;</span>
          <span>{readingTime} {minReadLabel}</span>
        </div>
        <Link href={`/blog/${slug}`} className="group">
          <h2 className="text-subheading group-hover:text-brand-600 transition-colors">
            {meta.title}
          </h2>
        </Link>
      </CardHeader>
      <CardContent className="flex flex-col flex-1">
        <p className="text-muted-foreground mb-4 flex-1">
          {meta.description}
        </p>
        <div className="flex flex-wrap gap-2 mb-4">
          {meta.tags.map((tag) => (
            <Badge key={tag} variant="secondary" className="text-xs">
              {tag}
            </Badge>
          ))}
        </div>
        <Link
          href={`/blog/${slug}`}
          className="text-sm font-medium text-brand-600 hover:text-brand-700 transition-colors"
        >
          {readMoreLabel} &rarr;
        </Link>
      </CardContent>
    </Card>
  );
}
```

Important: `PostCard` is a server component (no `'use client'`). It receives translated strings as props from the parent page. The `Link` component from `@/i18n/navigation` handles locale-prefixed routing automatically (same pattern as existing UseCaseCard).

Check if `@/i18n/navigation` exists in the project. If it does not exist, create it:

```typescript
// src/i18n/navigation.ts
import {createNavigation} from 'next-intl/navigation';
import {routing} from './routing';

export const {Link, redirect, usePathname, useRouter} = createNavigation(routing);
```

If it already exists, use the existing import. Check `src/i18n/navigation.ts` first.

**Step 5: Create `src/components/sections/blog/PostHeader.tsx`**

Server component for the top of individual blog post pages.

```typescript
import {Badge} from '@/components/ui/badge';
import {Separator} from '@/components/ui/separator';
import {Link} from '@/i18n/navigation';
import {ArrowLeft} from 'lucide-react';
import type {BlogPostMeta} from '@/lib/blog';
import {getReadingTime} from '@/lib/blog';
import {useLocale} from 'next-intl';

interface PostHeaderProps {
  meta: BlogPostMeta;
  content: string;
  backLabel: string;
  publishedLabel: string;
  byLabel: string;
  minReadLabel: string;
}

export function PostHeader({meta, content, backLabel, publishedLabel, byLabel, minReadLabel}: PostHeaderProps) {
  const locale = useLocale();
  const readingTime = getReadingTime(content);

  const formattedDate = new Intl.DateTimeFormat(locale, {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  }).format(new Date(meta.date));

  return (
    <header className="mb-8">
      <Link
        href="/blog"
        className="inline-flex items-center gap-1 text-sm text-muted-foreground hover:text-foreground transition-colors mb-8"
      >
        <ArrowLeft className="h-4 w-4" />
        {backLabel}
      </Link>

      <h1 className="text-display md:text-hero mb-4">
        {meta.title}
      </h1>

      <div className="flex flex-wrap items-center gap-3 text-sm text-muted-foreground mb-4">
        <span>{byLabel} {meta.author}</span>
        <span aria-hidden="true">&middot;</span>
        <time dateTime={meta.date}>{publishedLabel} {formattedDate}</time>
        <span aria-hidden="true">&middot;</span>
        <span>{readingTime} {minReadLabel}</span>
      </div>

      <div className="flex flex-wrap gap-2 mb-6">
        {meta.tags.map((tag) => (
          <Badge key={tag} variant="secondary" className="text-xs">
            {tag}
          </Badge>
        ))}
      </div>

      <Separator />
    </header>
  );
}
```

**Step 6: Create `src/components/sections/blog/TagFilter.tsx`**

This is a **client component** -- it uses `useRouter` and `useSearchParams` to update the URL query parameter when a tag is clicked.

```typescript
'use client';

import {useRouter, useSearchParams} from 'next/navigation';
import {Badge} from '@/components/ui/badge';
import {cn} from '@/lib/utils';

interface TagFilterProps {
  tags: string[];
  activeTag: string | null;
  allLabel: string;
  filterLabel: string;
}

export function TagFilter({tags, activeTag, allLabel, filterLabel}: TagFilterProps) {
  const router = useRouter();
  const searchParams = useSearchParams();

  function handleTagClick(tag: string | null) {
    const params = new URLSearchParams(searchParams.toString());
    if (tag) {
      params.set('tag', tag);
    } else {
      params.delete('tag');
    }
    // Reset to page 1 when changing tag filter
    params.delete('page');
    router.push(`?${params.toString()}`, {scroll: false});
  }

  return (
    <div className="mb-8" role="navigation" aria-label={filterLabel}>
      <div className="flex flex-wrap gap-2">
        <button onClick={() => handleTagClick(null)}>
          <Badge
            variant={activeTag === null ? 'default' : 'secondary'}
            className={cn(
              'cursor-pointer text-sm px-3 py-1',
              activeTag === null && 'bg-brand-600 text-white hover:bg-brand-700'
            )}
          >
            {allLabel}
          </Badge>
        </button>
        {tags.map((tag) => (
          <button key={tag} onClick={() => handleTagClick(tag)}>
            <Badge
              variant={activeTag === tag ? 'default' : 'secondary'}
              className={cn(
                'cursor-pointer text-sm px-3 py-1',
                activeTag === tag && 'bg-brand-600 text-white hover:bg-brand-700'
              )}
            >
              {tag}
            </Badge>
          </button>
        ))}
      </div>
    </div>
  );
}
```

Important: This uses `next/navigation` (not `@/i18n/navigation`) for `useRouter` and `useSearchParams` because these are client-side hooks. The `useRouter` from next/navigation works with the current locale prefix automatically in Next.js App Router.

Note the addition of `params.delete('page')` when changing tags -- this resets pagination to page 1 when the tag filter changes.

**Step 7: Create `src/components/sections/blog/BlogPagination.tsx`**

This is a **client component** for navigating between pages of blog posts.

```typescript
'use client';

import {useRouter, useSearchParams} from 'next/navigation';
import {Button} from '@/components/ui/button';
import {ChevronLeft, ChevronRight} from 'lucide-react';

interface BlogPaginationProps {
  currentPage: number;
  totalPages: number;
  previousLabel: string;
  nextLabel: string;
  pageOfLabel: string;
}

export function BlogPagination({
  currentPage,
  totalPages,
  previousLabel,
  nextLabel,
  pageOfLabel,
}: BlogPaginationProps) {
  const router = useRouter();
  const searchParams = useSearchParams();

  if (totalPages <= 1) return null;

  function goToPage(page: number) {
    const params = new URLSearchParams(searchParams.toString());
    if (page <= 1) {
      params.delete('page');
    } else {
      params.set('page', String(page));
    }
    router.push(`?${params.toString()}`, {scroll: false});
  }

  return (
    <div className="flex items-center justify-center gap-4 mt-12">
      <Button
        variant="outline"
        size="sm"
        onClick={() => goToPage(currentPage - 1)}
        disabled={currentPage <= 1}
        className="gap-1"
      >
        <ChevronLeft className="h-4 w-4" />
        {previousLabel}
      </Button>

      <span className="text-sm text-muted-foreground">
        {pageOfLabel}
      </span>

      <Button
        variant="outline"
        size="sm"
        onClick={() => goToPage(currentPage + 1)}
        disabled={currentPage >= totalPages}
        className="gap-1"
      >
        {nextLabel}
        <ChevronRight className="h-4 w-4" />
      </Button>
    </div>
  );
}
```

Key details:
- Returns `null` when there is only one page (no pagination needed).
- Page 1 removes the `?page` param entirely for clean URLs.
- Preserves existing `?tag` param when navigating pages.
- Uses `Button` from shadcn/ui with `variant="outline"` for consistent styling.
- `disabled` state on Previous (page 1) and Next (last page).
</instructions>
</task>

<task id="2">
<title>Build blog index page with pagination, blog post page, and RSS feed</title>
<instructions>
**Step 1: Replace blog stub with full blog index page (with pagination)**

Replace the entire contents of `src/app/[locale]/blog/page.tsx` with:

```typescript
import {setRequestLocale, getTranslations} from 'next-intl/server';
import {getAllPosts, getAllTags, getPostsByTag} from '@/lib/blog';
import {BlogHero} from '@/components/sections/blog/BlogHero';
import {PostCard} from '@/components/sections/blog/PostCard';
import {TagFilter} from '@/components/sections/blog/TagFilter';
import {BlogPagination} from '@/components/sections/blog/BlogPagination';
import {CTABanner} from '@/components/sections/CTABanner';

const POSTS_PER_PAGE = 9;

type Props = {
  params: Promise<{locale: string}>;
  searchParams: Promise<{tag?: string; page?: string}>;
};

export async function generateMetadata({params}: Props) {
  const {locale} = await params;
  const t = await getTranslations({locale, namespace: 'BlogPage'});
  return {
    title: `${t('metaTitle')} | Planifactor`,
    description: t('metaDescription'),
  };
}

export default async function BlogPage({params, searchParams}: Props) {
  const {locale} = await params;
  const {tag, page} = await searchParams;
  setRequestLocale(locale);
  const t = await getTranslations({locale, namespace: 'BlogPage'});

  const allTags = await getAllTags(locale);
  const allFilteredPosts = tag
    ? await getPostsByTag(locale, tag)
    : await getAllPosts(locale);

  // Pagination
  const currentPage = Math.max(1, parseInt(page || '1', 10) || 1);
  const totalPages = Math.max(1, Math.ceil(allFilteredPosts.length / POSTS_PER_PAGE));
  const safePage = Math.min(currentPage, totalPages);
  const startIndex = (safePage - 1) * POSTS_PER_PAGE;
  const posts = allFilteredPosts.slice(startIndex, startIndex + POSTS_PER_PAGE);

  return (
    <>
      <BlogHero />

      <section className="py-16 px-4">
        <div className="max-w-7xl mx-auto">
          <TagFilter
            tags={allTags}
            activeTag={tag || null}
            allLabel={t('allPosts')}
            filterLabel={t('filterByTag')}
          />

          {posts.length === 0 ? (
            <div className="text-center py-20">
              <p className="text-body-lg text-muted-foreground">
                {tag ? t('noPostsForTag') : t('noPosts')}
              </p>
            </div>
          ) : (
            <>
              <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                {posts.map((post) => (
                  <PostCard
                    key={post.slug}
                    slug={post.slug}
                    meta={post.meta}
                    content={post.content}
                    readMoreLabel={t('readMore')}
                    minReadLabel={t('minRead')}
                  />
                ))}
              </div>

              <BlogPagination
                currentPage={safePage}
                totalPages={totalPages}
                previousLabel={t('previousPage')}
                nextLabel={t('nextPage')}
                pageOfLabel={t('pageOf', {current: safePage, total: totalPages})}
              />
            </>
          )}
        </div>
      </section>

      <CTABanner />
    </>
  );
}
```

Key patterns matching existing codebase:
- `Props` type with `params: Promise<{locale: string}>` (same as all Phase 5 pages)
- `searchParams: Promise<{tag?: string; page?: string}>` for tag filtering and pagination (Next.js 16 makes searchParams a Promise)
- `setRequestLocale(locale)` called early (established i18n pattern)
- `generateMetadata` uses BlogPage namespace
- CTABanner at the bottom (same as use-cases, features pages)
- Grid layout: 1 col mobile, 2 cols md, 3 cols lg
- Pagination: 9 posts per page, `?page=N` searchParam, `BlogPagination` component shown below the grid
- `safePage` clamps the page number to valid range (prevents out-of-bounds)
- `BlogPagination` returns null when totalPages <= 1, so it auto-hides with few posts

**Step 2: Create blog post page at `src/app/[locale]/blog/[slug]/page.tsx`**

Create the `[slug]` directory and page file:

```typescript
import {setRequestLocale, getTranslations} from 'next-intl/server';
import {notFound} from 'next/navigation';
import {MDXRemote} from 'next-mdx-remote-client/rsc';
import remarkGfm from 'remark-gfm';
import rehypeSlug from 'rehype-slug';
import rehypePrettyCode from 'rehype-pretty-code';
import {getPostBySlug, getPostSlugs} from '@/lib/blog';
import {PostHeader} from '@/components/sections/blog/PostHeader';
import {CTABanner} from '@/components/sections/CTABanner';
import {mdxComponents} from '@/components/mdx/mdx-components';

type Props = {
  params: Promise<{locale: string; slug: string}>;
};

export async function generateStaticParams() {
  // Return slugs only -- parent [locale] handles locale enumeration
  // Gather unique slugs across all locales
  const locales = ['en', 'bg'];
  const slugSet = new Set<string>();
  for (const locale of locales) {
    const slugs = await getPostSlugs(locale);
    for (const slug of slugs) {
      slugSet.add(slug);
    }
  }
  return Array.from(slugSet).map((slug) => ({slug}));
}

export async function generateMetadata({params}: Props) {
  const {locale, slug} = await params;
  const post = await getPostBySlug(locale, slug);
  if (!post) return {};
  return {
    title: `${post.meta.title} | Planifactor`,
    description: post.meta.description,
    openGraph: {
      title: post.meta.title,
      description: post.meta.description,
      type: 'article',
      publishedTime: post.meta.date,
    },
  };
}

export default async function BlogPostPage({params}: Props) {
  const {locale, slug} = await params;
  setRequestLocale(locale);

  const post = await getPostBySlug(locale, slug);
  if (!post) notFound();

  const t = await getTranslations({locale, namespace: 'BlogPage'});

  return (
    <>
      <article className="max-w-3xl mx-auto py-16 px-4">
        <PostHeader
          meta={post.meta}
          content={post.content}
          backLabel={t('backToBlog')}
          publishedLabel={t('publishedOn')}
          byLabel={t('writtenBy')}
          minReadLabel={t('minRead')}
        />

        <div className="prose prose-lg max-w-none">
          <MDXRemote
            source={post.content}
            components={mdxComponents}
            options={{
              mdxOptions: {
                remarkPlugins: [remarkGfm],
                rehypePlugins: [
                  rehypeSlug,
                  [rehypePrettyCode, {theme: 'github-dark', keepBackground: true}],
                ],
              },
            }}
          />
        </div>
      </article>

      <CTABanner />
    </>
  );
}

export const dynamicParams = false;
```

Key implementation details:
- **`MDXRemote` from `next-mdx-remote-client/rsc`** compiles MDX at build/request time from the raw content string. This package is the dedicated RSC-compatible MDX renderer, built specifically for React Server Components and compatible with React 19.
- `mdxComponents` provides custom heading rendering and the Callout component.
- `remarkGfm` enables tables, strikethrough, task lists.
- `rehypeSlug` auto-generates heading IDs for anchor links.
- `rehypePrettyCode` with `github-dark` theme provides syntax highlighting (dark code blocks in light-mode site -- standard contrast pattern).
- `dynamicParams = false` prevents runtime slug resolution -- all slugs are from `generateStaticParams`.
- `generateStaticParams` returns unique slugs across locales (parent `[locale]` provides locale). Pattern matches use-cases `[slug]/page.tsx`.
- The `prose prose-lg max-w-none` classes apply Tailwind typography styling. `max-w-none` prevents the prose plugin from constraining width (the outer container already limits to `max-w-3xl`).

**Step 3: Create RSS feed route handler at `src/app/feed.xml/route.ts`**

Create the `feed.xml` directory and `route.ts` file inside it:

```typescript
import {Feed} from 'feed';
import {getAllPosts} from '@/lib/blog';

const SITE_URL = 'https://planifactor.com';

export async function GET() {
  const feed = new Feed({
    title: 'Planifactor Blog',
    description: 'Production scheduling insights for manufacturers',
    id: SITE_URL,
    link: `${SITE_URL}/en/blog`,
    language: 'en',
    copyright: `${new Date().getFullYear()} Planifactor`,
    feedLinks: {
      rss2: `${SITE_URL}/feed.xml`,
    },
  });

  const posts = await getAllPosts('en');

  for (const post of posts) {
    feed.addItem({
      title: post.meta.title,
      id: `${SITE_URL}/en/blog/${post.slug}`,
      link: `${SITE_URL}/en/blog/${post.slug}`,
      description: post.meta.description,
      date: new Date(post.meta.date),
      author: [{name: post.meta.author}],
    });
  }

  return new Response(feed.rss2(), {
    headers: {
      'Content-Type': 'application/rss+xml; charset=utf-8',
      'Cache-Control': 'public, max-age=3600, s-maxage=3600',
    },
  });
}
```

Key details:
- EN-only RSS feed (sufficient for MVP; BG feed can be added later).
- The `@/lib/blog` import works in route handlers -- Next.js App Router route handlers fully support tsconfig path aliases just like pages.
- The existing `proxy.ts` middleware matcher already excludes paths with file extensions (`.*\\..*`), so `/feed.xml` will NOT be processed by the i18n middleware. No middleware changes needed.
- `Cache-Control` header caches the feed for 1 hour.
- The route handler is at `src/app/feed.xml/route.ts` so it serves at `/feed.xml`.

**Step 4: Verify the full pipeline**

Run `npm run build` to verify:
1. The blog index page builds successfully with seed posts
2. The blog post pages are statically generated for all 4 seed posts (2 slugs x 2 locales)
3. The RSS feed route compiles
4. No TypeScript errors

Then start the dev server with `npm run dev` and verify:
1. Visit `/en/blog` -- should show 2 post cards with proper formatting
2. Visit `/bg/blog` -- should show 2 post cards in Bulgarian
3. Click a tag chip -- URL updates with `?tag=...` and posts filter correctly
4. Click "Read more" on a post card -- navigates to `/en/blog/production-scheduling-guide`
5. Blog post page shows: back link, title, date, author, tags, reading time, separator, then MDX content with prose typography
6. The `<Callout>` component renders inside the blog post content
7. Visit `/feed.xml` -- returns valid RSS XML with 2 entries
8. Tables in the AI manufacturing post render correctly (remark-gfm)
9. Mobile view (375px) renders the blog index and post pages properly
10. Pagination UI does NOT appear (only 2 posts, below the 9-post threshold) -- `BlogPagination` returns null when totalPages <= 1
</instructions>
</task>

## Verification

<verification>
Run the following commands to verify the blog is fully operational:

```bash
# 1. Verify BlogPage translations exist in both locales
node -e "
const en = require('./messages/en.json');
const bg = require('./messages/bg.json');
const required = ['metaTitle', 'heroTitle', 'allPosts', 'readMore', 'minRead', 'noPosts', 'backToBlog', 'publishedOn', 'writtenBy', 'previousPage', 'nextPage', 'pageOf'];
const enMissing = required.filter(k => !en.BlogPage?.[k]);
const bgMissing = required.filter(k => !bg.BlogPage?.[k]);
if (enMissing.length) { console.log('FAIL: EN missing keys:', enMissing); process.exit(1); }
if (bgMissing.length) { console.log('FAIL: BG missing keys:', bgMissing); process.exit(1); }
console.log('PASS: BlogPage translations complete in both locales (' + Object.keys(en.BlogPage).length + ' EN, ' + Object.keys(bg.BlogPage).length + ' BG keys)');
"

# 2. Verify all blog UI components exist
test -f src/components/sections/blog/BlogHero.tsx && echo "PASS: BlogHero" || echo "FAIL: BlogHero missing"
test -f src/components/sections/blog/PostCard.tsx && echo "PASS: PostCard" || echo "FAIL: PostCard missing"
test -f src/components/sections/blog/PostHeader.tsx && echo "PASS: PostHeader" || echo "FAIL: PostHeader missing"
test -f src/components/sections/blog/TagFilter.tsx && echo "PASS: TagFilter" || echo "FAIL: TagFilter missing"
test -f src/components/sections/blog/BlogPagination.tsx && echo "PASS: BlogPagination" || echo "FAIL: BlogPagination missing"

# 3. Verify blog pages exist
test -f src/app/[locale]/blog/page.tsx && echo "PASS: blog index page" || echo "FAIL: blog index page missing"
test -f "src/app/[locale]/blog/[slug]/page.tsx" && echo "PASS: blog post page" || echo "FAIL: blog post page missing"

# 4. Verify RSS route handler exists
test -f src/app/feed.xml/route.ts && echo "PASS: RSS route" || echo "FAIL: RSS route missing"

# 5. Verify MDXRemote import uses next-mdx-remote-client/rsc (NOT next-mdx-remote/rsc)
grep -q "next-mdx-remote-client/rsc" "src/app/[locale]/blog/[slug]/page.tsx" && echo "PASS: correct MDXRemote import" || echo "FAIL: wrong MDXRemote import"

# 6. Verify pagination is implemented in blog index
grep -q "POSTS_PER_PAGE" src/app/[locale]/blog/page.tsx && echo "PASS: pagination constant" || echo "FAIL: pagination missing"
grep -q "BlogPagination" src/app/[locale]/blog/page.tsx && echo "PASS: BlogPagination used" || echo "FAIL: BlogPagination not used"

# 7. Verify RSS route uses @/lib/blog import (path alias)
grep -q "@/lib/blog" src/app/feed.xml/route.ts && echo "PASS: RSS uses @/lib/blog" || echo "FAIL: RSS import wrong"

# 8. Full build -- all blog routes must compile and generate
npm run build

# 9. After build succeeds, verify static pages were generated (check build output for /en/blog and /bg/blog routes)
# The build output should list:
#   /en/blog
#   /en/blog/production-scheduling-guide
#   /en/blog/ai-in-manufacturing
#   /bg/blog
#   /bg/blog/production-scheduling-guide
#   /bg/blog/ai-in-manufacturing
#   /feed.xml

# 10. Start dev server and test RSS feed content type
# npm run dev &
# curl -s -o /dev/null -w "%{content_type}" http://localhost:3000/feed.xml | grep -q "rss+xml" && echo "PASS: RSS content type" || echo "FAIL: RSS content type wrong"
```

All checks above must pass. The build must complete with zero errors. The blog index, individual post pages, and RSS feed must all be generated.
</verification>

## must_haves

1. Blog index page at `/[locale]/blog` lists all published posts for the current locale, sorted by date descending, with tag filtering via URL searchParams and pagination via `?page=N` (9 posts per page, Previous/Next navigation buttons)
2. Individual blog post pages at `/[locale]/blog/[slug]` render MDX content with `next-mdx-remote-client/rsc`, proper prose typography, and custom MDX components (Callout, heading overrides)
3. Posts support tags and can be filtered on the index page -- clicking a tag shows only matching posts, clicking "All Posts" shows all; changing tags resets to page 1
4. RSS feed at `/feed.xml` returns valid RSS 2.0 XML listing all published English posts
5. Adding a new `.mdx` file to `src/content/blog/{locale}/` with valid YAML frontmatter automatically appears on the blog index after rebuild (no registry or config changes needed -- `fs.readdir` discovers posts dynamically)
6. All blog UI text (hero, labels, empty states, pagination labels) is fully translated in both EN and BG via the BlogPage translation namespace
